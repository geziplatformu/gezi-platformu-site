const grid=document.getElementById('tourGrid'),modal=document.getElementById('tourModal'),menuToggle=document.querySelector('.menu-toggle'),nav=document.querySelector('.main-nav');document.getElementById('year').textContent=new Date().getFullYear();menuToggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));function whatsappLink(t){return `https://wa.me/905374978441?text=${encodeURIComponent(`Merhaba, ${t.title} (${t.subtitle}) turu hakkında bilgi almak istiyorum.`)}`}function renderTours(){grid.innerHTML=window.TOURS.map(t=>`<article class="tour-card"><img class="tour-image" src="${t.image}" alt="${t.title} - ${t.subtitle}" loading="lazy"><div class="tour-body"><div class="tour-topline"><span class="badge">${t.badge}</span><span class="price">${t.price}</span></div><h3>${t.title}</h3><p>${t.subtitle}</p><div class="tour-meta"><span>${t.duration}</span><span>${t.departure}</span></div><div class="tour-actions"><button class="btn btn-outline" data-tour="${t.id}">Detay</button><a class="btn btn-primary" href="${whatsappLink(t)}" target="_blank" rel="noopener">Bilgi Al</a></div></div></article>`).join('');document.querySelectorAll('[data-tour]').forEach(b=>b.addEventListener('click',()=>openTour(b.dataset.tour)))}function openTour(id){const t=window.TOURS.find(x=>x.id===id);if(!t)return;modalImage.src=t.image;modalImage.alt=`${t.title} - ${t.subtitle}`;modalBadge.textContent=t.badge;modalTitle.textContent=`${t.title} — ${t.subtitle}`;modalSummary.textContent=t.summary;modalRoute.textContent=t.route;modalIncluded.textContent=t.included;modalMeta.innerHTML=`<span>${t.price}</span><span>${t.duration}</span><span>${t.departure}</span><span>${t.dates}</span>`;modalWhatsapp.href=whatsappLink(t);modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelectorAll('[data-close-modal]').forEach(el=>el.addEventListener('click',closeModal));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});renderTours();